pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
matplotlib>=3.7
networkx>=3.0
beautifulsoup4>=4.12
lxml>=4.9
pyarrow>=14.0

# Optional legacy / advanced retrieval dependencies.
# PyTerrier requires Java and can be fragile on laptops, so the main
# implementation includes pure-Python BM25 and TF-IDF fallbacks.
pyterrier>=1.0; platform_system != "Darwin"
pyterrier-alpha>=0.16; platform_system != "Darwin"
rank-bm25>=0.2.2

