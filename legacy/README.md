# Legacy classical IR work

The original project files are preserved at repository root:

- `final_search_engine.ipynb`
- `index_raw/`
- `index_tags/`
- `beir-index/`
- original `data/expanded_llm.csv`, `data/topics_df.csv`, and `data/topics_df_q.csv`

The new social-search implementation does not delete or overwrite these files.

